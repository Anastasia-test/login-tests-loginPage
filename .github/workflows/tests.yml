name: Run Selenium Tests

on:
  push:
  pull_request:
  schedule:
    - cron: "0 3 * * *"

jobs:
  selenium-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4   # ⬅️ обновили

      - name: Setup Node.js
        uses: actions/setup-node@v4   # ⬅️ обновили
        with:
          node-version: 18

      - name: Install Google Chrome
        run: |
          sudo apt update
          sudo apt install -y wget gnupg
          wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo apt install -y ./google-chrome-stable_current_amd64.deb

      - name: Install Dependencies
        run: npm install

      - name: Run Selenium Tests
        env:
          CHROME_PATH: /usr/bin/google-chrome
        run: node runAll.js

      - name: Upload Reports
        uses: actions/upload-artifact@v4   # ⬅️ ВОТ ГЛАВНОЕ ИСПРАВЛЕНИЕ
        with:
          name: selenium-reports
          path: reports/
